# 切换环境：local / remote / staging
DB_MODE=local

# 本地数据库连接参数（明确指定）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456

# 本地数据库库名
DB_ORDERS=car_transport2
DB_QUOTES=car_transport_quote
DB_AUDIT=car_transport_audit

# 应用端口
PORT=4010

# JWT 密钥（本地调试随便填）
JWT_SECRET=dev_secret_key
